# Frontend-only dependencies — Streamlit UI layer
# DO NOT add backend/ML packages here; those belong in requirements.txt (backend image only).
# Keeping this slim is what keeps the frontend image ~200 MB instead of ~2 GB.

# Core UI
streamlit>=1.37.0
plotly>=5.18.0
pydeck>=0.8.0

# Data handling
pandas>=2.1.4
numpy>=1.26.3
python-dateutil>=2.8.2

# HTTP (calls backend REST API)
requests>=2.31.0

# Config / env
python-dotenv>=1.0.0

# Timezone
pytz>=2024.1
